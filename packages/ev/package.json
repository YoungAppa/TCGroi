{
  "name": "@packroi/ev",
  "version": "0.1.0",
  "private": true,
  "description": "PACKROI's pure EV/ROI math engine. Zero dependencies, zero I/O — the same code runs in cron jobs and in the browser.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
